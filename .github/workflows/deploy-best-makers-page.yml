name: Deploy Best-Makers Comparison Page

on:
  push:
    branches: [main]
    paths:
      - 'pages/best-custom-soccer-jersey-makers-2026'
      - 'scripts/deploy-best-makers-page.js'
      - '.github/workflows/deploy-best-makers-page.yml'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not write to CMS)'
        required: false
        type: boolean
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Deploy best-makers comparison page
        env:
          OEMSAAS_TOKEN_EN: ${{ secrets.OEMSAAS_TOKEN_EN }}
          DRY_RUN: ${{ inputs.dry_run && 'true' || 'false' }}
        run: node scripts/deploy-best-makers-page.js
