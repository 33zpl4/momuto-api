name: Deploy ES Clubes & Academias

on:
  push:
    branches: [main]
    paths:
      - 'pages/equipaciones-para-clubes-academias'
      - 'scripts/deploy-clubes-academias-page.js'
      - '.github/workflows/deploy-clubes-academias-page.yml'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not write to CMS)'
        required: false
        type: boolean
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Deploy ES clubes y academias
        env:
          OEMSAAS_TOKEN_ES: ${{ secrets.OEMSAAS_TOKEN_ES }}
          DRY_RUN: ${{ inputs.dry_run && 'true' || 'false' }}
        run: node scripts/deploy-clubes-academias-page.js
