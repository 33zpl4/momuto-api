name: Deploy Youth & Club Page

on:
  push:
    branches: [main]
    paths:
      - 'pages/custom-youth-club-soccer-uniforms'
      - 'scripts/deploy-youth-club-page.js'
      - '.github/workflows/deploy-youth-club-page.yml'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not write to CMS)'
        required: false
        type: boolean
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Deploy youth & club page
        env:
          OEMSAAS_TOKEN_EN: ${{ secrets.OEMSAAS_TOKEN_EN }}
          DRY_RUN: ${{ inputs.dry_run && 'true' || 'false' }}
        run: node scripts/deploy-youth-club-page.js
