name: Deploy Blog Post

on:
  workflow_dispatch:
    inputs:
      post_handle:
        description: 'URL handle of the post (must match a file in blogs/)'
        required: true
      dry_run:
        description: 'Dry run — preview only, no writes to CMS'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Deploy blog post
        env:
          OEMSAAS_TOKEN_EN: ${{ secrets.OEMSAAS_TOKEN_EN }}
          POST_HANDLE: ${{ github.event.inputs.post_handle }}
          DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: node scripts/deploy-blog-post.js
